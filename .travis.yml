language: python
python:
    - "2.7"
    - "3.3"
    - "3.4"
    - "pypy"
install:
    - pip install -r requirements.txt -r test-requirements.txt
script:
    nosetests --with-coverage --cover-package=omx
after_success:
    coveralls
sudo: false
